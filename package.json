{
  "name": "node-webserver",
  "version": "1.0.2",
  "description": "An easy-to-use webserver powered by Node and ExpressJS",
  "main": "dist/src/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && node dist/src/app.js",
    "clean": "rimraf dist/",
    "copy-files": "copyfiles -u 1 ./src/**/*.html ./src/**/*.css ./src/**/*.saas ./src/**/*.png ./src/**/*.ico  ./src/**/*.jpg ./src/**/*.jpeg ./src/**/*.gif ./src/**/*.mp3 ./src/**/*.mp4 ./dist/src/",
    "build": "npm run clean && tsc && npm run copy-files && npm run minify",
    "minify": "uglifyjs dist/src/app.js -o dist/src/app.js -c -m"
  },
  "keywords": [],
  "author": "Logan Brown",
  "license": "ISC",
  "dependencies": {
    "cluster": "^0.7.7",
    "compression": "^1.7.4",
    "cookie-parser": "^1.4.6",
    "express": "^4.18.2",
    "express-rate-limit": "^6.7.0",
    "morgan": "^1.10.0",
    "path": "^0.12.7",
    "typescript": "^4.9.5",
    "uglify-js": "^3.17.4",
    "vhost": "^3.0.2"
  },
  "devDependencies": {
    "@types/node": "^18.14.1",
    "copyfiles": "^2.4.1",
    "rimraf": "^4.1.2"
  }
}
