{
  "name": "node-webserver",
  "version": "1.0.0",
  "description": "",
  "main": "dist/src/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && node dist/src/app.js",
    "clean": "rimraf dist/",
    "copy-files": "copyfiles -u 1 ./src/**/*.html ./src/**/*.css ./src/**/*.png ./src/**/*.jpg ./src/**/*.jpeg ./src/**/*.gif ./dist/src/",
    "build": "npm run clean && tsc && npm run copy-files"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cookie-parser": "^1.4.6",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-asyncify": "^1.1.0",
    "morgan": "^1.10.0",
    "path": "^0.12.7",
    "reload": "^3.2.1",
    "typescript": "^4.9.5"
  },
  "devDependencies": {
    "@types/node": "^18.14.1",
    "copyfiles": "^2.4.1",
    "rimraf": "^4.1.2"
  }
}
