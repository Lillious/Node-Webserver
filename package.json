{
  "name": "node-webserver",
  "version": "1.0.1",
  "description": "An easy-to-use webserver powered by Node and ExpressJS",
  "main": "dist/src/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && node dist/src/app.js",
    "clean": "rimraf dist/",
    "copy-files": "copyfiles -u 1 ./src/**/*.html ./src/**/*.css ./src/**/*.png ./src/**/*.jpg ./src/**/*.jpeg ./src/**/*.gif ./src/**/*.mp3 ./src/**/*.mp4 ./dist/src/",
    "build": "npm run clean && tsc && npm run copy-files"
  },
  "keywords": [],
  "author": "Logan Brown",
  "license": "ISC",
  "dependencies": {
    "cluster": "^0.7.7",
    "cookie-parser": "^1.4.6",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-asyncify": "^1.1.0",
    "helmet": "^6.0.1",
    "morgan": "^1.10.0",
    "path": "^0.12.7",
    "reload": "^3.2.1",
    "typescript": "^4.9.5"
  },
  "devDependencies": {
    "@types/node": "^18.14.1",
    "copyfiles": "^2.4.1",
    "rimraf": "^4.1.2"
  }
}
